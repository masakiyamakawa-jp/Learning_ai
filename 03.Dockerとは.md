# Dockerの概要
- Docker（ドッカー）は、アプリケーションを動かすための環境を丸ごとパッケージ化して、どこでも同じように動作させるためのツール
- アプリを入れ物（コンテナ）に詰めて、どのPCでも同じように実行できるようにする技術」

[特徴]
- 仮想マシンより軽く、高速に起動できる
- GitHub ActionsやJenkinsでDockerを使ってテスト・デプロイを自動化
- サーバーにDockerイメージを送るだけでアプリを配備


# なぜ必要か？
- 現代の開発では「動く環境が人やマシンで違う」問題がよく起こる
- Dockerを使うと、OS・ライブラリ・依存関係すべてを1つのコンテナに封じ込めるので、
どこで実行しても 「同じ環境で動く」 ことが保証されます

# Dockerの仕組み
- 以下の３要素で動作
  
* イメージ      - 実行環境の設計図（例：Python 3.10 + FastAPI入り）
* コンテナ      - イメージを実際に動かした実体（＝アプリそのもの）
* Dockerfile    - イメージを作るためのレシピファイル
* docker-compose.yml    - 複数のサービス（例：フロント＋バック）を一緒に起動する設定ファイル




# バックエンドとフロントエンドを別々のDockerコンテナにして、docker-compose up コマンド1つでまとめて起動

C:\Ai_Learning\Projects\
 ├─ backend/
 │   ├─ main.py
 │   ├─ requirements.txt（FastAPIなど）
 │   └─ Dockerfile
 │
 ├─ frontend/
 │   ├─ src/
 │   │   ├─ App.js
 │   │   ├─ Home.js
 │   │   └─ Login.js
 │   ├─ package.json
 │   └─ Dockerfile
 │
 └─ docker-compose.yml



[起動方法]
1. docker engineを起動
2. docker compose up -d
    - 複数のコンテナをまとめて起動
    - -d オプションでバックグラウンド実行
3. ブラウザから http://localhost:3000 にアクセス
    - docker-compose.yml の ports: に書いたホスト側ポートでアクセス

[コンテナ確認]
1. docker ps        - 動作中のコンテナ表示
2. docker ps -a     - 全てのコンテナ表示
*一度コンテナ化されたものが表示される*


[image確認]
1. docker images    - イメージの一覧


# イメージのビルド
docker build -t myapp .

# コンテナ起動
docker run -d -p 8000:8000 myapp
(-d: バックグラウンド実行, -p: ポートフォワード[ホスト:コンテナ])

# Dockerfile
- コンテナの設計図で、環境を自動で構築する手順
- このファイルを元にDocker imageを作成
- docker build コマンドでイメージ作成


# requirements.txt
- アプリケーションの動作に必要な Rython ライブラリの一覧
- Dockerファイルが参照し、依存関係をインストール


# docker-compose.yml
- コンテナをまとめて管理する設定ファイル
- 複数のコンテナをまとめて起動・停止・連携する
- docker compose コマンドで複数のコンテナをまとめてビルド・起動・管理(docker-compose.ymlを参照)
- docker run コマンドで単一のコンテナを起動




- docker compose build --no-cache frontend
    - キャッシュなしでdockerイメージを再構成
    - [用途]
      - キャッシュが効いて変更が反映されない時
      - キャッシュ由来のエラーを疑うトラブルシュート時
      - ベースイメージや依存関係を絶対に取り直したい時