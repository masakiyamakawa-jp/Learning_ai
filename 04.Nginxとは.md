# Nginxの概要

- 「Webサーバー／リバースプロキシ」（静的ファイル配信や通信中継をする）


⚙️ 2. では、Nginxはどこから登場するのか？
✅ パターン1：開発環境（npm start）

開発中は、Reactが持っている 開発用サーバー（Viteやwebpack-dev-server） が動いています。

npm start や yarn dev を実行すると、ポート3000で立ち上がる

これは Nginxではなく、Node.js が立てている簡易サーバー

ソースコードの変更を自動反映（Hot Reload）してくれます

つまりこの段階では：

ブラウザ → (localhost:3000) → React Dev Server


で動いており、Nginxは使っていません。

✅ パターン2：本番構成（ビルド後）

Reactアプリを npm run build すると、
index.html や main.js といった静的ファイルが出力されます。
これを配信するためにNginxを使うのが一般的です。

本番時のイメージ：

ブラウザ
  ↓
http://yourdomain.com  → Nginx（静的ファイル配信 & /apiをFastAPIに中継）
                                       ↓
                                     FastAPI（バックエンド）


このときのNginxは、次の2つの仕事をします：

Reactのビルド成果物を配信
（例：/var/www/html/index.html や JSファイルを返す）

バックエンドAPIへのリクエストを中継（リバースプロキシ）
→ こうすることでCORS問題を解消できます。




[問題点]
[2025/11/07] - /docker-compose.yml の REACT_APP_API_BASE: "http://backend:8000" が原因でフロント→バックの通信が失敗
- 改善策1: すぐ直るが、CORS 設定によりプリフライドが走る
  - フロントの API ベースURLを http://localhost:8000 に変更
  - FastAPI に CORS を必ず入れる（http://localhost:3000 を許可）
  - 再ビルド&再起動
- 改善策2: Nginx で /api をバックへリバースプロキシ(本番より)
  - REACT_APP_API_BASE を相対パス(または "/api")に変更
  - frontコンテナの Nginx設定リバースプロキシ設定